# Firmware host tests

Fast, host-native unit tests for the firmware's pure-logic and cryptographic
**contracts** — the parts that are cheap to get subtly wrong and expensive to
debug in the field once an image is signed and shipped:

| Suite | Mirrors | What it pins |
|-------|---------|--------------|
| `test_version_compare.c` | `ota_manager.c` `ota_parse_mmp` / `ota_is_downgrade` | OTA version ordering + downgrade guard, against a golden table shared with the server (`src/lib/firmware.ts` `compareSemver`) |
| `test_ota_signature.c` | `ota_manager.c` `verify_one_key` | Ed25519 (PURE-EdDSA, RFC 8032) verify over the 32-byte image digest — valid accepts, tampered digest/sig and wrong key reject |
| `test_secure_channel.c` | `secure_channel.c` `secure_channel_decrypt_token` | X25519 ECDH → HKDF-SHA256 (info `vellum-token-v1`) → AES-256-GCM (`ct‖tag`) token decrypt — valid recovers, tampered/wrong-key/wrong-nonce reject |

## Why host-native, not ESP-IDF `set-target linux`

These test **contracts**, not drivers, so they don't need the ESP-IDF toolchain.
Building with the system compiler + OpenSSL keeps the suite fast (seconds, no
container), dependency-light, and locally reproducible. The device itself uses
the PSA Crypto API (mbedtls); OpenSSL here implements the same standardized
primitives, and the golden vectors are generated by the **server** signer/encrypt
path, so a drift on *either* side of the contract fails the test.

## Run

```sh
cmake -S firmware/host_test -B firmware/host_test/build
cmake --build firmware/host_test/build
ctest --test-dir firmware/host_test/build --output-on-failure
```

Requires CMake ≥ 3.16, a C11 compiler, and OpenSSL dev headers
(`apt-get install libssl-dev` / `brew install openssl@3`).

## Regenerating the golden vectors

The vectors baked into the `test_*.c` files come from `scripts/gen_kat.mjs`
(Node), which drives the same X25519/HKDF/AES-GCM and Ed25519 constructions the
server uses. If the crypto contract intentionally changes, regenerate and paste
the new constants:

```sh
node firmware/host_test/scripts/gen_kat.mjs
```

CI runs this suite via `.github/workflows/firmware-host-test.yml` on every push
and PR that touches `firmware/**`.
